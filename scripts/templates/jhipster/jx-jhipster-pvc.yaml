kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jx-jhipster-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: X_STORAGE_X
  storageClassName: standard
#  volumeName: jx-hipster-pv
